display_name=Read XML
short_description=Reads an XML file content and loads it into the step's XML. |  The <i>Read XML</i> step reads any XML file and loads its content. As a consequence, the content of the XML file is available as a source for other following steps.