property.httpInfo.display_name=HTTP info
property.httpInfo.short_description=Defines whether to include HTTP information in output XML. | HTTP information can be added to the transaction's output XML, such as the request URL, HTTP status code and the HTTP request and response headers. You can also have the raw HTTP data in case of error. \n\
The {{Produit/Fonction}}HTTP info{{-Produit/Fonction}} property allows to define whether these information have to be inserted in the transaction's output XML (value set to {{Computer}}true{{-Computer}}) or not (value set to {{Computer}}false{{-Computer}}). \n\
Default value is {{Computer}}false{{-Computer}}.
property.httpInfoTagName.display_name=HTTP info tagname
property.httpInfoTagName.short_description=Defines the tagname of the element containing the HTTP info in output XML. | When the {{Produit/Fonction}}HTTP info{{-Produit/Fonction}} property defines to insert the HTTP information in the transaction's output XML, the {{Produit/Fonction}}HTTP info tagname{{-Produit/Fonction}} property allows the programmer to define the tagname of the element containing these information. \n\
Default value is {{Computer}}HttpInfo{{-Computer}}.
property.httpVerb.display_name=HTTP verb
property.httpVerb.short_description=Defines the HTTP verb to use for this HTTP request: {{Computer}}GET{{-Computer}}, {{Computer}}POST{{-Computer}}, {{Computer}}PUT{{-Computer}}, {{Computer}}DELETE{{-Computer}}, {{Computer}}HEAD{{-Computer}}, {{Computer}}TRACE{{-Computer}}, {{Computer}}OPTIONS{{-Computer}} or {{Computer}}CONNECT{{-Computer}}. | For more information about HTTP verbs, you can visit the following RFC page: {{Reference}}http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html{{-Reference}}.
property.subDir.display_name=Sub path
property.subDir.short_description=Defines the end of the path for the HTTP connection. | This property allows to define the sub path, relative to the connector root path, to the target web service URI.\n\
For example, if the target is: {{Computer}}http://server/MyApp/targetpage.jsp{{-Computer}}, the connector server would be: {{Computer}}server{{-Computer}}, the connector root path: {{Computer}}/MyApp{{-Computer}} and the transaction sub path: {{Computer}}/targetpage.jsp{{-Computer}}.
property.handleCookie.display_name=Handles cookies
property.handleCookie.short_description=Defines whether cookies must be handled. | If set to {{Computer}}true{{-Computer}} (default value), the transaction maintains cookies in Convertigo's context. Default value should not be changed unless you specifically want the transaction to ignore cookies while browsing. 
property.httpParameters.display_name=HTTP headers
property.httpParameters.short_description=Defines HTTP headers to be sent. | This property allows to define the request Header Fields to be sent with the request to the target web service method. \n\
For each header, two columns have to be set:\
***{{Produit/Fonction}}Variable{{-Produit/Fonction}}: HTTP header name (ex: {{Computer}}Content-Type{{-Computer}}).\
***{{Produit/Fonction}}Value{{-Produit/Fonction}}: HTTP header value (ex: {{Computer}}application/x-www-from-urlencoded{{-Computer}}). \n\
{{Orange Twinsoft}}Note:{{-Orange Twinsoft}} A new HTTP header can be added to the list using the blue keyboard icon. The HTTP headers defined in the list can be ordered using the arrow up and arrow down buttons, or deleted using the red cross icon.
#property.httpVariables.display_name=HTTP variables
#property.httpVariables.short_description=Defines conversions between transaction variables and HTTP variables required by the target server. | Transaction input variables. Each input variable has its own set of properties:\
#***Variable: Variable name.\
#***Description: Variable description.\
#***Default value: Variable default value if no input value is sent to the transaction.\
#***WSDL: If set to {{Computer}}true{{-Computer}}, includes this variable in the generated SOAP WSDL as an input variable for this method (transaction) of the webservice.\
#***Multi: Multi valued variables can be input more than once per request. Also, they are inserted into the transaction as JavaScript arrays, instead of strings.\
#***Personalizable: When adding this transaction as a widget in Convertigo Mashup Composer, personalizable variables are added to the widget as user preferences.\
#***Cached key: When considering cache usage to serve a request, Convertigo only compares cached key variables between the new request and the cached one to determine whether cache should be used or not.\
#***Method: HTTP method to be used for this variable if the transaction connector is to auto-connect (i.e. when {{Produit/Fonction}}Maintain connector state{{-Produit/Fonction}} is false) and if an HTTP variable name is defined for this variable.\
#***HTTP variable: If an HTTP variable name is defined, then this variable value is sent as an HTTP variable upon connector auto-connect (see {{Produit/Fonction}}Maintain connector state{{-Produit/Fonction}} parameter).
property.requestTemplate.display_name=Request template
property.requestTemplate.short_description=Defines the request body template file path. | HTTP request sent by the transaction can contain data in its body. This data is based on a user-defined template file, which can be: \
***an XML file describing the content of the HTTP request body, possibly including transaction input variables in the data structure, \
***an XSL file used to transform the variable-based transaction input XML to generate the content of the HTTP request body. \n\
This property allows to define the path of the template file, it is either: \
***a local file, by default relative to the project's directory, or to the project's current subfolder, \
***a local file relative to the Convertigo webapp common {{Computer}}templates{{-Computer}} directory, \
***an absolute path. \n\
If the template file is an XML file, it can contain transaction variables identified with a specific syntax in the XML and dynamically replaced at runtime with received variable values. \n\
The syntax to use in the XML template file to refer to a transaction variable is the following: \
***{{Computer}}$(<variableHttpName>){{-Computer}}: this simple notation starts with a {{Computer}}${{-Computer}} character and then includes between brackets the {{Produit/Fonction}}HTTP name{{-Produit/Fonction}} of the variable. Beware that the {{Produit/Fonction}}HTTP name{{-Produit/Fonction}} of the variable can be different from the variable name (see {{Reference}}Variable{{-Reference}} objects documentation). \
***{{Computer}}$(<variableHttpName>)concat{{-Computer}}: this notation is very similar to the preceding, excepted that the last bracket is followed by the {{Computer}}concat{{-Computer}} keyword. It starts by a {{Computer}}${{-Computer}} character and includes between brackets the {{Produit/Fonction}}HTTP name{{-Produit/Fonction}} of the variable, that should be in this case a {{Reference}}Multi-valued variable{{-Reference}}. The {{Computer}}concat{{-Computer}} keyword implies that all values received in the {{Reference}}Multi-valued variable{{-Reference}} must be concatenated before replacing this notation by this computed value in the template XML. \
***{{Computer}}$(<variableHttpName>){{-Computer}}: this notation is identical to the first notation, but the behavior is different for a {{Reference}}Multi-valued variable{{-Reference}}. The tag surrounding this notation in the template XML is duplicated for each value in the {{Reference}}Multi-valued variable{{-Reference}}.  
property.urlEncodingCharset.display_name=URL charset encoding
property.urlEncodingCharset.short_description=Defines the charset encoding to use for the variable values sent as parameters in HTTP request. | This property allows to define the charset encoding used to URL-encode the parameter values: \
***GET parameters for the query string, \
***POST parameters in case of {{Computer}}application/x-www-form-urlencoded{{-Computer}} content-type. \n\
Default value is blank. If blank, the parent connector's {{Produit/Fonction}}Default URL charset encoding{{-Produit/Fonction}} property value is used.
