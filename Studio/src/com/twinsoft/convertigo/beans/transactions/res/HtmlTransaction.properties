display_name=HTML transaction
short_description=Defines an HTML transaction. | As regards Web applications, Convertigo's basic principle is to:\
***connect to Web sites, \
***detect defined screen classes and navigate through Web screens,\
***extract Web screen data using screen class-specific extraction rules,\
***treat extracted data in order to generate an XML document structured as required.\n\
This process defines the notion of {{Reference}}HTML transaction{{-Reference}}. \n\
{{Reference}}HTML transactions{{-Reference}} are based on input variables, as other transactions. They execute actions that are programmed using event handlers and statements, they are designed to handle Web pages navigation. {{Reference}}HTML Transactions{{-Reference}} return data in an XML structure resulting from automated navigation on the target web site and extraction rules execution on these pages or, in the case of {{Reference}}Web Clipper{{-Reference}} extraction rule, clip required HTML parts of the Web pages.\n\
A Convertigo project can contain several {{Reference}}HTML transactions{{-Reference}}, each of them being exposed as standard REST or SOAP web service method.
property.stateFull.display_name=Maintains connector state
property.stateFull.short_description=Defines whether the connector state must be maintained between transactions. | If set to {{Computer}}false{{-Computer}} (default value), each time the transaction is executed, it connects the connector browser to the target Web page specified in the {{Produit/Fonction}}Sub path{{-Produit/Fonction}} property using the {{Produit/Fonction}}HTTP headers{{-Produit/Fonction}} property for headers, and the {{Produit/Fonction}}Synchronization{{-Produit/Fonction}} property to synchronize on the accessed page.\n\
If set to {{Computer}}true{{-Computer}}, the transaction maintains the connector state, i.e. uses the browser in the state in which it has been left when the preceding transaction ended. 
property.trigger.display_name=Synchronization
property.trigger.short_description=Defines how to synchronize the transaction. | HTML transactions can connect automatically (without using any specific statement) to a target web page (see the {{Produit/Fonction}}Maintain connector state{{-Produit/Fonction}} property). This property allows to specify which type of synchronizer is used in the connection process.\n\
A synchronizer states how and when accessed pages are considered fully loaded. Only then are data extracted and new pages re-detected. \n\
There are several types of synchronizers, that are described hereafter:\
***{{Computer}}Document completed{{-Computer}}: The synchronizer waits for a number of documents to be completed. Specify here how many "document completed" events Convertigo has to wait for before assuming that the page is complete. \
In many cases, when the target application uses HTTP META redirects or JavaScript redirects, the document is loaded several times. You can monitor \n\
{{Computer}}==== start parse ======{{-Computer}}\n\
and\n\
{{Computer}}==== Parse end ==(XXXms )===================={{-Computer}}\n\
traces in the Engine console (debug mode) to count the number of "document completed" events needed for the synchronizer. \
The {{Computer}}Document completed{{-Computer}} synchronizer can be configured to also stop on alert messages that could pop up. Alert messages do not trigger a "document completed" event and are not detected by this synchronizer. To activate this option, check the {{Produit/Fonction}}Stop on alert{{-Produit/Fonction}} checkbox. \
***{{Computer}}XPath{{-Computer}}: The synchronizer waits until a specified XPath is found. Convertigo tries to evaluate the specified XPath while receiving a Web page or executing JavaScript in it. Once the XPath matches at least one node of the page, the synchronizer returns. \
***{{Computer}}Wait time{{-Computer}}: The synchronizer waits until a specified time is reached (in ms, set via the {{Produit/Fonction}}Timeout{{-Produit/Fonction}} property). \
***{{Computer}}Screen Class{{-Computer}}: The synchronizer waits for one of the selected screen classes to be detected. Several screen classes can be selected to be waited for. The synchronizer returns when one of them is reached. \
***{{Computer}}Download started{{-Computer}}: The synchronizer waits for a download request. This is the perfect synchronizer to use before a {{Produit/Fonction}}Get attachment{{-Produit/Fonction}} statement. \
***{{Computer}}No wait{{-Computer}}: The synchronizer doesn't wait and execution proceeds directly. \n\
For all synchronizer types, the maximum waiting time is set using the {{Produit/Fonction}}Timeout{{-Produit/Fonction}} property.
property.subDir.display_name=Sub path
property.subDir.short_description=Defines the end of the path to use for connecting to the target Web page. | {{Reference}}HTML transaction{{-Reference}} can connect automatically to a target web page without using a specific statement (see the {{Produit/Fonction}}Maintain connector state{{-Produit/Fonction}} property). The {{Produit/Fonction}}Sub path{{-Produit/Fonction}} property allows to define the sub path, relative to the connector root path, to connect to the target web page URI.\n\
For example, if the target is: {{Computer}}http://server/MyApp/targetpage.jsp{{-Computer}}, the connector server would be: {{Computer}}server{{-Computer}}, the connector root path: {{Computer}}/MyApp{{-Computer}} and the transaction sub path: {{Computer}}/targetpage.jsp{{-Computer}}.
property.httpParameters.display_name=HTTP headers
property.httpParameters.short_description=Defines HTTP headers to be sent. | {{Reference}}HTML transaction{{-Reference}} can connect automatically to a target web page without using a specific statement (see the {{Produit/Fonction}}Maintain connector state{{-Produit/Fonction}} property). The {{Produit/Fonction}}HTTP headers{{-Produit/Fonction}} property allows to define the request Header Fields to be sent with the request to connect to the target web page. \n\
For each header, two colums have to be parametered:\
***{{Produit/Fonction}}Variable{{-Produit/Fonction}}: HTTP header name (ex: {{Computer}}Content-Type{{-Computer}}).\
***{{Produit/Fonction}}Value{{-Produit/Fonction}}: HTTP header value (ex: {{Computer}}application/x-www-from-urlencoded{{-Computer}}). 
